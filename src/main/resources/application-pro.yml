spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/minggblog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: qq2890340612

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.mingg.minggblog: info
  file:
    name: blog-pro.log
    path: log/

server:
  port: 8081
